# Общее представление библиотеки
`geometric_lib` предоставляет набор утилит для работы с числовыми значениями геометрических фигур. 
## Философия библиотеки
В основе решения лежит функциональное программирование. Данная библиотека предоставляет функции для работы с конкретными числовыми значениями группированными по названиям класса геометрических фигур. Основная цель библиотеки - быть удобным и читабильным дополнением к базовым вычислениям - избавить пользователя от необходимость реализовывать тривиальные функции базовых формул геометрии в своём проекте тем самым делая его код чище.

# Документация по наполнению
## Квадраты
Вычислительные функции связанные с квадратами находятся в модуле `square.py`.

---

### Площадь
```py
def area(a: float|int) -> float|int:
    """
    Вычисляет площадь квадрата с длинной ребра a. Возвращаемый тип данных соответствует типу данных параметра a.

    Параметры:
        a (float|int) - длина ребра квадрата.

    Возвращает:
        S (float|int) - площаль квадрата.
    """
    ...
```
Пример вызова:
```py
>>> import geometric_lib.square as square

>>> a = square.area(100)
>>> print(a, type(a))

10000 <class 'int'> # Результат и класс возвращаемого значения

>>> a = square.area(3.14)
>>> print(a, type(a))

9.8596 <class 'float'> 
```

---

### Периметр
```py
def perimeter(a: float|int) -> float|int:
    """
    Вычисляет периметр квадрата с длинной ребра a. Возвращаемый тип данных соответствует типу данных параметра a.

    Параметры:
        a (float|int) - длина ребра квадрата.

    Возвращает:
        S (float|int) - периметр квадрата.
    """
    ...
```
Пример вызова:
```py
>>> import geometric_lib.square as square

>>> a = square.perimeter(100)
>>> print(a, type(a))

400 <class 'int'> # Результат и класс возвращаемого значения

>>> a = square.perimeter(3.14)
>>> print(a, type(a))

12.56 <class 'float'>
```

---

## Круги и окружности
Вычислительные функции связанные с квадратами находятся в модуле `circle.py`.

---

### Площадь
```py
def area(r: float) -> float:
    """
    Вычисляет площадь круга радиусом r. 

    Параметры:
        r (float) - радиус круга.

    Возвращает:
        S (float) - площаль круга.
    """
    ...
```
Пример вызова: 
```py
>>> import geometric_lib.circle as circle
>>> circle.area(3.14)

30.974846927333928 # Всегда float
```

---

### Периметр / длина окружности
```py
def perimeter(r: float) -> float:
    """
    Вычисляет периметр круга/длину окружности радиусом r. 

    Параметры:
        r (float) - радиус круга/окружности.

    Возвращает:
        S (float) - периметр круга/длина окружности.
    """
    ...
```
Пример вызова: 
```py
>>> import geometric_lib.circle as circle
>>> circle.perimeter(3.14)

19.729201864543903 # Всегда float
```

# История
## Pre alpha
Добавлена реализация модулей `circle.py` и `square.py`. (smartiqa -> `8ba9aeb3cea847b63a91ac378a2a6db758682460`)

Добавлена документация по формулам (smartiqa -> `d078c8d9ee6155f3cb0e577d28d337b791de28e2`)

## Alpha
### v0.1a
Добавлена документация к функциям модулей `circle.py` и `square.py`. (LeastDepressedDev -> `dfa7c489c63418b4644072fc9f6620966a471ea4`, `505070600926b815e3695b0d15c2aaded817659d`)

# Tail
Версия документации: 0.1.2a